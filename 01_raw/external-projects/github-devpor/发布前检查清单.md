# 发布前检查清单

## 1. 功能检查

- [ ] 搜索功能可返回仓库列表（关键词与 URL 两种方式）
- [ ] README 可正确加载并展示
- [ ] 下载流程可开始/显示进度/取消
- [x] 支付 Mock 流程可跑通（创建 -> paid -> 验单）
- [x] 付费模式下安装执行有扣次，次数不足会阻断
  - 说明：已通过 `npm run smoke` 完成自动烟测，UI 交互仍需三轮手工确认

## 2. 配置与安全检查

- [x] `Server Base URL` 指向可用环境（当前 `:8788`）
- [x] `AI Base URL` 指向可用 OpenAI 兼容接口
- [ ] API Key 仅保存本地，不提交到仓库
- [x] `.local/`、密钥等敏感文件已在 `.gitignore` 中排除
  - 说明：当前目录尚未初始化为 Git 仓库，无法执行 `git status` 进行提交面复核

## 3. 构建与测试检查

- [x] 本地 `npm run build` 通过
- [x] 本地 `npm test` 通过
- [ ] 关键 UI 操作无阻塞性错误（至少完整跑 3 次）

## 4. 服务器检查

- [x] `http://47.109.187.147:8788/api/health` 可访问
- [x] `pm2 status` 显示 `github-devpor-server` 为 `online`
- [x] `pm2-logrotate` 为 `online`
- [x] 日志目录大小正常（无异常膨胀）

## 5. 交付检查

- [x] `README.md`、`最小交付说明.md`、`发布前检查清单.md` 已更新
- [x] `开发进度与状态.md` 已记录最新状态
- [x] 已知限制已明确告知（Mock 支付、最小安全规则等）

## 6. 待手工回归项

- 搜索、README、下载流程需要在桌面客户端界面手工完整跑通并记录 3 轮结果（模板见 `手工UI三轮回归记录.md`）
- API Key 本地保存与未提交验证需在交付前执行一次 `git status` 人工复核（前提：目录已初始化 Git）
