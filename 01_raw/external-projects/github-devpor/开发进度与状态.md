# github-devpor 开发进度与状态

## 项目状态

- 当前阶段: `M1 开发中（第1步已落地）`
- 当前焦点: `手工回归执行准备`
- 最近更新时间: `2026-04-28`

## 里程碑状态

- M1 核心可用: `进行中`
- M2 懒人安装: `未开始`
- M3 支付授权: `未开始`
- M4 稳定性与发布: `未开始`

## 当前迭代任务（进行中）

1. 按模板执行手工 UI 三轮回归并记录
2. 继续观察远程守护与日志轮转稳定性
3. 完成交付前敏感信息复核
4. 准备发布提交

## 今日变更记录

### 2026-04-28
- 新增设计文档与 README
- 补充了按 M1~M4 的可执行任务清单
- 初始化 npm 工程并安装核心依赖
- 新增本地忽略文件用于存放服务器连接信息
- 客户端骨架完成：Electron + React + preload IPC + 设置存储
- 本地验证通过：`npm run build`、`npm test`
- 新增服务端骨架：`server/index.js`（健康检查、订单、验证、代理列表）
- 完成第1步：搜索与 README 真实链路已接入 GitHub API
- 新增模型最小能力：`Say hello` 测试接口、最小安装计划生成接口
- 新增最小执行器：安装命令逐条执行（基础黑名单 + 超时限制）
- 新增下载模块最小链路：任务创建、进度事件回传、取消
- 新增远程部署脚本：`scripts/deploy-server.ps1`（打包、上传、远程安装与启动）
- 模型链路调整：降级为可选，不作为当前主流程阻塞
- 支付 Mock 客户端链路接入：创建订单/查状态/模拟回调/验单
- 新增设备标识与服务端地址配置：`deviceId`、`serverBaseUrl`
- 远程部署完成：`47.109.187.147` 已上传并启动服务（端口 `8788`）
- 远程验证结果：服务器本机 `127.0.0.1:8788/api/health` 正常返回；公网直连当前为 `502`（需后续排查网关/转发）
- 模型接口切换：默认改为 `http://47.109.187.147:5001/v1/chat/completions`（后续可替换域名）
- 模型连通验证通过：`Say hello` 返回 `Hello! How can I help you today?`
- 公网健康检查通过：`http://47.109.187.147:8788/api/health` 可正常访问
- 支付前置校验已接入：仅 `lazy + builtin` 模式允许触发支付接口
- 支付自动轮询已接入：新增 `Auto Poll + Verify`（pending 自动轮询至 paid 后验单）
- 支付与安装联动前置检查已接入：
  - `verify/use` 成功后本地 `remainingPaidUses +1`
  - `install:executeMinimal` 在付费模式下执行前检查剩余次数，执行时自动扣减
  - 次数不足时阻断执行并返回明确错误
- 前端新增本地剩余次数展示与调试入口（刷新/重置），便于联调验证
- 新增自动化测试：`tests/server-payment-flow.test.js`（覆盖创建订单 -> 查询 -> 回调 -> 验单）
- 远程守护进程已接入：`pm2` 成功托管 `github-devpor-server`，状态 `online`
- 新增守护脚本：`scripts/setup-remote-pm2.ps1`（无 root，本地 pm2 + npx 启动）
- 新增安装扣次自动化测试：`tests/payment-usage.test.ts`（支付判定与扣次规则）
- 测试框架升级：`npm test` 支持 JS + TS 测试（`node --test` + `tsx --test`）
- 日志轮转已落地：远程 `pm2-logrotate` 安装并启用（10M、保留14份、压缩、每日轮转）
- 新增日志轮转脚本：`scripts/setup-remote-logrotate.ps1`
- 支付页面新增一键闭环按钮：`Run Full Mock Flow`（创建 -> 回调 -> 验单）
- 调试入口收口：剩余次数手动改写仅在 `Show Debug Tools` 开启时显示
- 安装执行后会同步刷新本地剩余次数显示，便于观察扣次结果
- 新增交付文档：`最小交付说明.md`
- 新增发布清单：`发布前检查清单.md`
- README 文档索引已更新，加入交付说明与发布清单
- 远程稳定性快照：
  - `github-devpor-server`（pm2）`online`，连续运行约 28 分钟
  - `pm2-logrotate` `online`，配置已生效（10M/14份/压缩/每日轮转）
  - 日志体积当前正常（`server-out.log` 63B，`server-error.log` 0B）
- 发布清单已完成自动化勾选：
  - 构建/测试通过
  - 远程健康检查、pm2、logrotate 状态通过
  - OpenAI 兼容模型连通通过（`Say hello`）
  - `.gitignore` 已覆盖 `.local/`
- 发布清单保留手工项：
  - 客户端 UI 三轮回归（搜索/README/下载）
  - 交付前 API Key 本地保存与未提交人工复核
- 新增手工回归模板：`手工UI三轮回归记录.md`（三轮可直接勾选执行）
- 新增自动烟测脚本：`npm run smoke`（搜索/README/模型/支付/远程健康自动检查）
- 自动烟测结果：`6` 项中 `5 pass`、`1 warn`、`0 fail`
  - 告警项：下载小文件受网络波动影响（`ECONNRESET`），已标记非阻断并保留手工回归
- 远程状态快照（本轮）：
  - `github-devpor-server` uptime 约 29 分钟，`online`
  - `pm2-logrotate` `online`，配置保持生效
  - 日志目录体积 `8.0K`，无异常增长
- 新增发布前复核说明：
  - 当前目录尚未识别为 Git 仓库，`git status` 复核需在 Git 初始化后执行
  - 已先完成 `.gitignore` 敏感目录排除检查（`.local/`）
- 自动烟测已提升为全通过：`npm run smoke` 当前 `6 pass / 0 warn / 0 fail`
- 新增交付结论：`交付结论.md`（汇总已完成项、剩余人工项与建议发布动作）
- 远程状态快照（最新）：
  - `github-devpor-server` uptime 约 36 分钟，`online`
  - `pm2-logrotate` `online`
  - `logs` 目录体积稳定（约 `4.0K`）
- 本轮验证通过：`npm run build`、`npm test`
- 模型直连测试结果：`baoapi.com` 在当前网络下出现 ICP 拦截页/HTTPS 超时，暂未获得有效模型文本输出

## 阻塞与风险

- Electron 二进制下载在默认源存在超时风险，需要使用国内镜像
- 服务器仅国内公网，某些国外源依赖需要本地下载后上传
- 原 `baoapi.com` 当前在本地网络不可稳定访问，已临时切换至 `47.109.187.147:5001`
- 远程 `8787` 端口被现有服务占用，当前临时使用 `8788`
- 远程公网 `8788` 已可访问，但仍需补一轮稳定性回归
- 需观察 `pm2-logrotate` 首次轮转结果与磁盘占用趋势

## 下一步计划

1. 完成手工 UI 三轮回归并回填 `发布前检查清单.md`
2. 执行 `git status` 完成敏感信息未提交复核（待 Git 初始化）
3. 继续观察 `pm2-logrotate` 轮转结果（至少跨 1 个轮转点）

## 2026-04-28 UI重构补全与重打包

- 按用户提供布局重做为暗色卡片式界面（顶部信息区 + 参数区 + 左列表右详情 + 底部日志）
- 新增语言切换入口（Chinese / English），界面文案随语言切换
- 明确移除 SteamTools 相关字段和按钮，不再出现在主界面
- 补全完整功能入口：设置保存、搜索/README、安装计划与执行、下载启动/取消、支付流程与调试工具
- 处理编码构建故障：定位到 `tsx` 文件中非 UTF-8 字节（历史编码污染），改为 ASCII 内容后构建恢复
- 本轮验证通过：`npm run build`
- 当前进行中：Windows 安装包重打（`npm run dist:win`）
- 支付下单链路开始重构：
  - 一键安装模式点击安装时直接创建订单
  - 服务端订单接口扩展为双通道二维码（微信/支付宝）
  - 客户端新增支付订单卡片，展示订单号、金额、状态、过期时间与双二维码
  - 客户端已接入订单状态自动轮询（每 3 秒刷新）
- 支付链路补全（本轮）：
  - 订单卡片新增“模拟已支付”按钮（本地联调）
  - 订单状态变为 `paid` 后自动触发 `verify/use`
  - 验证成功后自动继续下载/安装后续流程（当前接入自动下载步骤）
- 支付展示交互调整（本轮）：
  - 移除独立“支付订单卡片”
  - 点击安装后改为弹窗展示微信/支付宝二维码
  - 下单前同步保存当前设置，避免“模式已切换但未保存”导致的 `createOrder` 模式误判
- 一键安装链路补全（本轮）：
  - 下载进度总量为 0 时显示“未知总量”，避免误解为不显示进度
  - 支付成功后串联“下载完成 -> 自动执行最小安装流程”
- 一键安装体验修复（本轮）：
  - 总量未知时不再显示百分比，改为“已下载大小 + 未知总量”
  - 内置模式未配置 `aiApiKey` 时，`install:planMinimal` 提供本地兜底安装计划，避免流程中断
- 内置模型凭据策略调整（本轮）：
  - 内置模型改为固定使用本地约定的 Endpoint / Model / API Key
  - 内置模式不再依赖设置页中的 `aiApiKey` 输入
- 服务端密钥写入与安装规划链路重构（本轮）：
  - 支付完成后将订单号写入 `/home/shx/ds2api/config.json` 的 `api_keys` 第一组（含时间戳和备注）
  - 写入流程新增“锁文件 + 队列串行”机制，避免并发订单冲突写入
  - 客户端安装规划改为仅上传 `apikey(订单号)` 给服务端，由服务端调用模型并返回安装步骤
- 安装规划分流规则调整（本轮）：
  - 内置模型：上传订单号作为 `apikey`，由服务端调用模型
  - 自定义模型：继续走 OpenAI 兼容直连调用模式（不依赖订单号）
- 安装异常策略调整（本轮）：
  - `install:planMinimal` 返回 404 时客户端不再本地兜底安装
  - 改为暂停安装流程并调用退款接口，将订单状态更新为 `refunded`
  - 服务端新增 `/api/order/refund` 接口
