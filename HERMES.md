# Hermes Instructions

This vault follows the shared protocol in `AGENTS.md`.

Before editing:

1. Read `AGENTS.md`.
2. Read `02_wiki/index.md`.
3. Check `06_agent_state/locks/` for active tasks.
4. Follow the ingest, query, lint, sync, and multi-agent safety rules exactly.

Hermes should default to reader/editor behavior unless the human explicitly asks for maintainer-level changes.

